import Hero from './hero';
import Content from './content';

const Blog = () => {
  return (
    <main className="flex-1 relative">
      <Hero />
      <Content />
    </main>
  );
};

export default Blog;
